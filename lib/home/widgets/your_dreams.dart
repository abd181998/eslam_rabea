import 'package:flutter/material.dart';

class YourDreams extends StatelessWidget {
  const YourDreams({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset('assets/images/search.png'),
    );
  }
}
