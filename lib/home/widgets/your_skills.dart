import 'package:flutter/material.dart';

class YourSkills extends StatelessWidget {
  const YourSkills({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child:  Image.asset('assets/images/search.png'),
    );
  }
}
